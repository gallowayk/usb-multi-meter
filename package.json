{
  "name": "usb-multi-meter",
  "version": "1.0.0",
  "description": "Multi-device USB BLE power meter with synchronized readings",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.20",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "chart.js": "^4.4.0"
  }
}
